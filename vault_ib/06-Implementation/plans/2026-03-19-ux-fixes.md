# UX Fixes Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모바일 내비게이션 진입점 추가, group-hover 버그 수정, 클릭 div→Link 교체, 폼 라벨 추가 — 4개 UX/접근성 이슈 수정.

**Architecture:** 각 수정은 독립적. 공유 컴포넌트(`auth-layout`, `keyword-picker`) 1~2줄 수정이 대부분. `idea-card`와 `idea-list`는 `<div onClick>` → `<Link>` 래퍼 패턴으로 교체.

**Tech Stack:** Next.js 15 App Router, shadcn/ui SidebarTrigger, Tailwind CSS

---

## 수정 대상 파일 목록

| 파일 | 수정 내용 |
|------|----------|
| `src/components/auth-layout.tsx` | 모바일 헤더 + SidebarTrigger 추가 |
| `src/components/keyword-picker.tsx:83` | button에 `group` 클래스 추가 |
| `src/components/idea-card.tsx:42` | `<div onClick>` → `<Link>` |
| `src/components/idea-list.tsx:36,84` | `<div onClick>` → `<Link>` |
| `src/app/keywords/page.tsx:85,92` | Input/Select에 label 추가 |

---

## Task 1: 모바일 내비게이션 — SidebarTrigger 헤더 추가

**Files:**
- Modify: `src/components/auth-layout.tsx`

**현재 구조:**
```tsx
<SidebarInset>
  <main className="flex-1 px-4 py-6 ...">
    {children}
  </main>
</SidebarInset>
```

**문제:** 모바일에서 사이드바를 열 버튼이 없음. `SidebarTrigger`는 구현돼 있지만(`sidebar.tsx:254`) 렌더되는 곳이 없음.

**수정 후 구조:**
```tsx
<SidebarInset>
  {/* 모바일 전용 상단 헤더 */}
  <header className="flex md:hidden items-center gap-3 px-4 py-3 border-b border-border bg-background sticky top-0 z-40">
    <SidebarTrigger />
    <span className="text-sm font-bold text-foreground">Idea Bank</span>
  </header>
  <main className="flex-1 px-4 py-6 sm:px-6 md:px-10 md:py-8 bg-background min-h-screen max-w-7xl mx-auto">
    {children}
  </main>
</SidebarInset>
```

- [ ] **Step 1: `auth-layout.tsx` 수정**

`src/components/auth-layout.tsx` 전체를 아래로 교체:

```tsx
"use client";

import { usePathname } from "next/navigation";
import { SidebarProvider, SidebarInset, SidebarTrigger } from "@/components/ui/sidebar";
import { TooltipProvider } from "@/components/ui/tooltip";
import { AppSidebar } from "@/components/app-sidebar";

export function AuthLayout({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();
  const isLoginPage = pathname === "/login";

  if (isLoginPage) {
    return <TooltipProvider>{children}</TooltipProvider>;
  }

  return (
    <TooltipProvider>
      <SidebarProvider>
        <AppSidebar />
        <SidebarInset>
          {/* 모바일 전용 상단 헤더 — md 이상에서는 사이드바가 항상 보임 */}
          <header className="flex md:hidden items-center gap-3 px-4 py-3 border-b border-border bg-background sticky top-0 z-40">
            <SidebarTrigger />
            <span className="text-sm font-bold text-foreground">Idea Bank</span>
          </header>
          <main className="flex-1 px-4 py-6 sm:px-6 md:px-10 md:py-8 bg-background min-h-screen max-w-7xl mx-auto">
            {children}
          </main>
        </SidebarInset>
      </SidebarProvider>
    </TooltipProvider>
  );
}
```

- [ ] **Step 2: TypeScript 확인**

```bash
npx tsc --noEmit && echo "✓ OK"
```
Expected: `✓ OK`

- [ ] **Step 3: 커밋**

```bash
git add src/components/auth-layout.tsx
git commit -m "fix: add mobile sidebar trigger header for navigation"
```

---

## Task 2: group-hover 버그 수정 — keyword-picker.tsx

**Files:**
- Modify: `src/components/keyword-picker.tsx:83`

**문제:**
- `line 83` button: `group` 클래스 없음
- `line 103` Plus 아이콘: `group-hover:opacity-100` 사용 → 부모에 `group` 없으면 절대 작동 안 함
- 결과: 비선택 키워드의 `+` 아이콘이 항상 `opacity-0`

**현재:**
```tsx
<button
  key={keyword.id}
  onClick={() => onToggle(keyword)}
  className={cn(
    "floating-bubble flex items-center gap-2.5 rounded-full px-6 py-3 border text-sm font-semibold transition-all",
    isSelected ? ... : "bg-surface shadow-marshmallow border-white/60 text-text-main hover:shadow-marshmallow-hover",
  )}
>
```

**수정:** button className에 `group` 추가.

- [ ] **Step 1: `keyword-picker.tsx` button에 `group` 추가**

`src/components/keyword-picker.tsx:83`의 button className 첫 줄 변경:

```tsx
"group floating-bubble flex items-center gap-2.5 rounded-full px-6 py-3 border text-sm font-semibold transition-all",
```

(`"floating-bubble flex..."` → `"group floating-bubble flex..."`)

- [ ] **Step 2: TypeScript 확인**

```bash
npx tsc --noEmit && echo "✓ OK"
```

- [ ] **Step 3: 커밋**

```bash
git add src/components/keyword-picker.tsx
git commit -m "fix: add group class to keyword picker button for hover feedback"
```

---

## Task 3: 클릭 가능한 div → Link 교체

**Files:**
- Modify: `src/components/idea-card.tsx`
- Modify: `src/components/idea-list.tsx`

### 3-1. `idea-card.tsx`

**현재 (line 42):**
```tsx
<div
  onClick={handleClick}
  className={cn("... cursor-pointer ...")}
>
```
`handleClick`은 `router.push(\`/ideas/${idea.id}\`)`를 호출.

**수정 후:** 외부 `<div>` → `<Link href={...}>`, `router` import 제거.

완성 코드:
```tsx
"use client";

import Link from "next/link";
import { cn } from "@/lib/utils";
import { CategoryBadge } from "@/components/category-badge";
import { Bookmark, BookmarkCheck, AlertTriangle } from "lucide-react";
import type { KeywordCategory, IdeaStatus } from "@/types";
import { STATUS_LABELS } from "@/lib/constants";
import { Badge } from "@/components/ui/badge";

interface IdeaCardProps {
  idea: {
    id: string;
    rank?: number;
    title: string;
    summary: string;
    target_user?: string;
    problem?: string;
    keywords_used?: string[];
    bookmarked: boolean;
    total_score?: number | null;
    status?: IdeaStatus;
    duplicate_warning?: boolean;
  };
  onBookmarkToggle?: (id: string, bookmarked: boolean) => void;
  compact?: boolean;
}

export function IdeaCard({ idea, onBookmarkToggle, compact }: IdeaCardProps) {
  const handleBookmark = (e: React.MouseEvent) => {
    e.preventDefault();  // Link 내부에서 클릭 이벤트 버블링 방지
    onBookmarkToggle?.(idea.id, !idea.bookmarked);
  };

  return (
    <Link
      href={`/ideas/${idea.id}`}
      className={cn(
        "block bg-card text-card-foreground rounded-card-lg shadow-marshmallow border border-border cursor-pointer transition-all duration-300 hover:shadow-marshmallow-hover hover:-translate-y-0.5",
        compact ? "p-4" : "p-6",
      )}
    >
      {/* Header */}
      <div className="flex items-start justify-between gap-3">
        <div className="flex items-center gap-2 min-w-0">
          {idea.rank != null && (
            <span className="flex size-6 shrink-0 items-center justify-center rounded-full bg-primary/10 text-xs font-bold text-primary">
              {idea.rank}
            </span>
          )}
          <h3
            className={cn(
              "font-bold text-foreground truncate",
              compact ? "text-sm" : "text-lg",
            )}
          >
            {idea.title}
          </h3>
        </div>

        <div className="flex items-center gap-1 shrink-0">
          {idea.total_score != null && (
            <span
              className={cn(
                "rounded-full px-2 py-0.5 text-xs font-bold",
                idea.total_score >= 80
                  ? "bg-score-high-bg text-score-high-text"
                  : idea.total_score >= 60
                    ? "bg-score-mid-bg text-score-mid-text"
                    : "bg-score-low-bg text-score-low-text",
              )}
            >
              {idea.total_score}점
            </span>
          )}
          {idea.duplicate_warning && (
            <AlertTriangle className="size-4 text-amber-500" />
          )}
          <button
            onClick={handleBookmark}
            aria-label={idea.bookmarked ? "북마크 해제" : "북마크"}
            aria-pressed={idea.bookmarked}
            className="min-w-10 min-h-10 flex items-center justify-center text-muted-foreground hover:text-primary transition-colors rounded-full hover:bg-muted/50"
          >
            {idea.bookmarked ? (
              <BookmarkCheck className="size-5 text-primary fill-primary/20" />
            ) : (
              <Bookmark className="size-5" />
            )}
          </button>
        </div>
      </div>

      {/* Summary */}
      <p
        className={cn(
          "text-muted-foreground mt-2",
          compact ? "text-xs line-clamp-2" : "text-sm line-clamp-3",
        )}
      >
        {idea.summary}
      </p>

      {/* Status badge (compact mode) */}
      {compact && idea.status && (
        <div className="mt-2">
          <Badge variant="secondary" className="text-xs">
            {STATUS_LABELS[idea.status]}
          </Badge>
        </div>
      )}

      {/* Footer: target user + keywords (full mode) */}
      {!compact && (
        <div className="mt-4 flex flex-wrap items-center gap-2">
          {idea.target_user && (
            <span className="text-xs text-muted-foreground bg-muted rounded-full px-2.5 py-1">
              {idea.target_user}
            </span>
          )}
          {idea.keywords_used?.slice(0, 3).map((kw) => (
            <CategoryBadge
              key={kw}
              category={guessCategoryFromKeyword(kw)}
              label={kw}
              className="text-xs"
            />
          ))}
        </div>
      )}
    </Link>
  );
}

/** Best-effort category guess for display purposes. Defaults to "domain". */
function guessCategoryFromKeyword(_keyword: string): KeywordCategory {
  return "domain";
}
```

> **핵심 변경점:**
> - `useRouter` import 제거, `Link` import 추가
> - 외부 `<div onClick>` → `<Link href>`로 교체
> - 북마크 button의 `e.stopPropagation()` → `e.preventDefault()` 로 변경 (`<Link>` 내부에서는 `preventDefault`가 네비게이션 차단)

### 3-2. `idea-list.tsx`

**현재 (line 36, 84):** Desktop row + Mobile card 모두 `<div onClick={router.push}>`

**수정:** 두 `<div onClick>` 모두 `<Link href>` 로 교체. 내부 북마크 버튼은 `e.preventDefault()` 사용.

- [ ] **Step 1: `idea-card.tsx` 전체 교체**

위 완성 코드로 `src/components/idea-card.tsx` 전체 교체.

- [ ] **Step 2: `idea-list.tsx` Desktop row (line 36) 수정**

```tsx
// 변경 전
<div
  onClick={() => router.push(`/ideas/${idea.id}`)}
  className="hidden md:grid grid-cols-[1fr_100px_60px_80px_100px] gap-4 items-center bg-surface rounded-card shadow-marshmallow p-4 cursor-pointer transition-all duration-300 hover:shadow-marshmallow-hover hover:-translate-y-0.5"
>

// 변경 후
<Link
  href={`/ideas/${idea.id}`}
  className="hidden md:grid grid-cols-[1fr_100px_60px_80px_100px] gap-4 items-center bg-surface rounded-card shadow-marshmallow p-4 cursor-pointer transition-all duration-300 hover:shadow-marshmallow-hover hover:-translate-y-0.5"
>
```

- [ ] **Step 3: `idea-list.tsx` Mobile card (line 84) 수정**

```tsx
// 변경 전
<div
  onClick={() => router.push(`/ideas/${idea.id}`)}
  className="md:hidden bg-surface rounded-card-lg shadow-marshmallow p-4 cursor-pointer transition-all duration-300 hover:shadow-marshmallow-hover"
>

// 변경 후
<Link
  href={`/ideas/${idea.id}`}
  className="md:hidden bg-surface rounded-card-lg shadow-marshmallow p-4 cursor-pointer transition-all duration-300 hover:shadow-marshmallow-hover"
>
```

- [ ] **Step 4: `idea-list.tsx` 북마크 버튼 `stopPropagation` → `preventDefault`**

`idea-list.tsx` 내 BookmarkButton onClick 핸들러:
```tsx
// 변경 전
e.stopPropagation();
// 변경 후
e.preventDefault();
```
(두 곳 모두 — desktop row와 mobile card)

- [ ] **Step 5: `idea-list.tsx` import 정리**

```tsx
// 추가
import Link from "next/link";
// 제거 (useRouter 더 이상 불필요)
import { useRouter } from "next/navigation";
// useRouter 사용 부분도 제거
const router = useRouter();
```

- [ ] **Step 6: TypeScript 확인**

```bash
npx tsc --noEmit && echo "✓ OK"
```

- [ ] **Step 7: 커밋**

```bash
git add src/components/idea-card.tsx src/components/idea-list.tsx
git commit -m "fix: replace clickable div with Link in idea-card and idea-list"
```

---

## Task 4: 키워드 폼 라벨 추가

**Files:**
- Modify: `src/app/keywords/page.tsx`

**현재 (line 84-106):**
```tsx
<div className="flex gap-3">
  <Input placeholder="새 키워드 입력" className="flex-1" ... />
  <Select ...>
    <SelectTrigger className="w-32">
```

`<Input>`과 `<Select>`에 연결된 `<label>`이 없어서 스크린리더와 클릭 영역이 제한됨.

**수정 후:** form 구조로 감싸고, visually-hidden label 대신 form 상단에 명시적 라벨 추가. 입력창과 카테고리 선택이 나란히 있으므로 각각 `aria-label` 방식으로 처리.

```tsx
<div className="flex gap-3">
  <div className="flex-1 space-y-1">
    <label htmlFor="new-keyword" className="text-xs font-semibold text-text-muted uppercase tracking-wide">
      키워드
    </label>
    <Input
      id="new-keyword"
      value={newKeyword}
      onChange={(e) => setNewKeyword(e.target.value)}
      placeholder="새 키워드 입력"
      onKeyDown={(e) => e.key === "Enter" && handleAdd()}
    />
  </div>
  <div className="space-y-1">
    <label htmlFor="new-category" className="text-xs font-semibold text-text-muted uppercase tracking-wide">
      카테고리
    </label>
    <Select
      value={newCategory}
      onValueChange={(v) => setNewCategory(v as KeywordCategory)}
    >
      <SelectTrigger id="new-category" className="w-32">
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        {CATEGORY_ORDER.map((cat) => (
          <SelectItem key={cat} value={cat}>
            {cat.charAt(0).toUpperCase() + cat.slice(1)}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  </div>
  <div className="flex items-end">
    <Button onClick={handleAdd} disabled={addLoading || !newKeyword.trim()}>
      <Plus className="size-4" />
      <span>{addLoading ? "추가 중..." : "추가"}</span>
    </Button>
  </div>
</div>
```

- [ ] **Step 1: `keywords/page.tsx` form 영역 수정**

`src/app/keywords/page.tsx`의 `<div className="flex gap-3">` 블록(line 84-111)을 위 코드로 교체.

- [ ] **Step 2: TypeScript 확인**

```bash
npx tsc --noEmit && echo "✓ OK"
```

- [ ] **Step 3: 커밋**

```bash
git add src/app/keywords/page.tsx
git commit -m "fix: add labels to keyword add form for accessibility"
```

---

## 최종 검증

```bash
# 전체 TypeScript 확인
npx tsc --noEmit && echo "✓ OK"
```

**시각 확인 체크리스트:**
```
[ ] 모바일(375px 너비)에서 상단 햄버거 버튼 → 사이드바 슬라이드 열림 확인
[ ] /generate 키워드 피커에서 비선택 키워드 hover → + 아이콘 표시 확인
[ ] 아이디어 카드 Cmd+클릭 → 새 탭에서 열림 확인
[ ] 아이디어 카드 내 북마크 버튼 클릭 → 페이지 이동 안 하고 북마크만 토글 확인
[ ] /keywords 페이지 "키워드" 라벨 클릭 → input 포커스 이동 확인
[ ] /keywords 페이지 "카테고리" 라벨 클릭 → select 포커스 이동 확인
```
